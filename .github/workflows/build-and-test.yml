name: Build and test

on:
  pull_request:
  push:
    branches:
      - main

env:
  EXEC: "" # execute outside of Docker in CI

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres_test # "_test" is required for fixtures
        ports:
          - 5432/tcp
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      # Accepts every challenge without resolving anything
      # (PEBBLE_VA_ALWAYS_VALID). Used by the pkg/certmgr test, whose provider
      # doesn't publish DNS records. There is no pebble-dns-check counterpart
      # here: nothing in CI runs the sonar DNS server for it to resolve against.
      pebble-no-dns-check:
        image: ghcr.io/letsencrypt/pebble:latest
        env:
          PEBBLE_VA_NOSLEEP: 1
          PEBBLE_VA_ALWAYS_VALID: 1
          PEBBLE_AUTHZREUSE: 0
          PEBBLE_WFE_NONCEREJECT: 0
        ports:
          - 14000/tcp

    steps:
      - name: Install Golang
        uses: actions/setup-go@v5
        with:
          go-version: ${{ vars.GO_VERSION }}

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        run: make build

      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.13.2

      - name: Test
        run: make test
        env:
          SONAR_DB_DSN: 'postgres://postgres:postgres@localhost:${{ job.services.postgres.ports[5432] }}/postgres_test?sslmode=disable'
          SONAR_DB_MIGRATIONS: internal/database/migrations
          CERTMGR_CA_DIR_URL: "https://localhost:${{ job.services['pebble-no-dns-check'].ports[14000] }}/dir"

      - name: Code coverage
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.out
